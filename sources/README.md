# Sources

Approved, sanitized source records are immutable after creation. Corrections
must create new review or metadata records rather than rewriting a source file.

The TLDR wiki compiler accepts explicitly approved JSON source records under
`sources/tldr/`, including JSON content transported in `.txt` files.
