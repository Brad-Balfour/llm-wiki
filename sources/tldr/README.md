# Approved TLDR Sources

Place reviewed `approved-wiki-source.v1` JSON records here. A record's
`source.source_path` must exactly match its repository-relative filename.

Do not store raw Gmail bodies in this directory. Once a source record is
created, treat it as immutable.
